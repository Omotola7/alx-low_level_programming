#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char b;

	b = 'a';
	while
		(b <= 'm') {
			if ((b != 'q' && b != 'e') && b <= 'm')
				putchar(b);
			b++;
		}
	putchar('\n');
	return (0);
}
