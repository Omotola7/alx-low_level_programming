#include <stdio.h>
/**
 * main - Entry point
 * Return: to 0
 */

int main(void)
{
	char b;

	int e;

	b = 'a';
	while
		(e < 10)
		{
			putchar(e + '0');
			e++;
		}
	while
		(b <= 'f')
		{
			putchar(b);
			b++;
		}
	putchar('\n');
	return (0);
}
