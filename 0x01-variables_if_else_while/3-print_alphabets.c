#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char n;

	char m;

	n = 'a';
	m = 'A';
	while
		(n <= 'z') {
			putchar(n);
			n++;
		}
	while
		(m <= 'z') {
			putchar(n);
			m++;
		}
	putchar('\n');
	return (0);
}
